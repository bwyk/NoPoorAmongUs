@{
    ViewData["Title"] = "Note Types";
}
<div class="container">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Note Types</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="StudentNoteType" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp; Create Note Type
            </a>
        </div>
    </div>
    <br /><br />

    <table class="table table-bordered" style="width:100%">
        <thead>
            <th>
                Id
            </th>
            <th>
                Type
            </th>
        </thead>
        <tbody>
            @foreach(var obj in Model)
        {
            <tr>
                <td width="5%">
                    @obj.Id
                </td>
                <td width="65%">
                    @obj.Type
                </td>
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="StudentNoteType" asp-action="Edit" asp-route-id="@obj.Id"
                        class="btn btn-primary mx-2"> <i class="bi bi-pencil-square"></i> Edit</a>
                        <a asp-controller="StudentNoteType" asp-action="Delete" asp-route-id="@obj.Id"
                        class="btn btn-danger mx-2"> <i class="bi bi-trash-fill"></i> Delete</a>
                    </div>
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>
